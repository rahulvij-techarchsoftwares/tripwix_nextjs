import { SlideProps } from '~/components/Sliders/SliderManager/types';

export interface FoundersSliderProps {
  slides: SlideProps[];
}

export interface MemberCardProps {
  name: string;
  role: string;
  image: string;
}

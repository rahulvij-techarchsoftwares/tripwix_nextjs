const blockPrefix = 'intro_block_';

export enum IntroBlockVariants {
  TEXT_CENTER = `${blockPrefix}text_center`,
  TEXT_LEFT = `${blockPrefix}text_left`,
  TEXT_CENTER_GRAY_BG = `${blockPrefix}text_center_gray_bg`,
  GUIDE = `${blockPrefix}guide`,
  CONTACT = `${blockPrefix}contact`,
  FLEXCONTENT = `${blockPrefix}flex_content`,
  FLEXCONTENT_TOP_ALIGNED = `${blockPrefix}flex_content_top_aligned`,
  PAGECONTENT = `${blockPrefix}page_content`,
}
